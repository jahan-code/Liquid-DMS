const { errorCode } = require('./error.constants')
const { randomConstant } = require('./random.contants')
const { responseCode } = require('./response.contants');

module.exports = {
    responseCode,
    errorCode,
    randomConstant,
};